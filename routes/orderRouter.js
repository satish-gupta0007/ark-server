// import express from "express";
// import { isAuthenticated } from "../middlewares/auth.js";
// // import { addNotification, deleteNotification, getNotification, getNotificationDetails, getNotificationRoleWise, updategNotification } from "../controllers/notificationController.js";

// const orderRouter = express.Router();
// orderRouter.post("/add-order",isAuthenticated, addNotification);
// orderRouter.put("/get-all-orders/:userId",isAuthenticated, updategNotification);

// export default orderRouter;
